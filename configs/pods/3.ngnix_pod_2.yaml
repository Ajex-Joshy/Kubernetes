# This is auto generated using this command
# kubectl run nginx --image=nginx --dry-run=client -o yaml > configs/pods/ngnix_pod_2.yaml

# --dry-run=client :- Simulates the command locally using kubectl without sending the request to the Kubernetes API server. Mainly used to generate YAML manifests.
# --dry-run=server :- Sends the request to the Kubernetes API server for validation, but the resource is not actually created or stored in etcd.

# -o yaml :- output the resource definition in yaml format

apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
spec:
  containers:
    - image: nginx
      name: nginx
      resources: {} # used to define CPU and memory requirements for a container.
  dnsPolicy: ClusterFirst # How DNS resolution works for the pod, here Kubernetes DNS resolves these names if not found it fallback to external DNS
  restartPolicy: Always # Defines when containers should be restarted
status: {} # Current runtime state of the pod (filled by Kubernetes automatically

