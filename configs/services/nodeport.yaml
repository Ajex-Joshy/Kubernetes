apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport-svc
spec:
  type: NodePort

  selector:
    app: nginx

  ports:
    - protocol: TCP
      port: 80 # service port inside cluster
      targetPort: 80 # container port
      nodePort: 30007 # external port exposed on node
